import { SplitterItem } from "./components/splitter-item/splitter-item"

interface IParamsCalculateSizeSplitterItem {
    firstItem: SplitterItem,
    secondItem: SplitterItem,
    offset: number,
}

const calculateSizeSplitterItem = ({ firstItem, secondItem, offset }: IParamsCalculateSizeSplitterItem) => {

}